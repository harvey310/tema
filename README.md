# 分析看板部署说明

这是一个纯静态页面，不需要后端、不需要数据库。

## 部署文件

- `index.html`：线上入口文件

## 部署方式

把整个 `deploy` 目录上传到任意静态网站服务即可，例如：

- GitHub Pages
- Vercel
- Netlify
- 宝塔 / Nginx 静态站点
- OSS / COS 静态网站托管

## 数据说明

页面只内置 2026 年初始化数据。新增数据会保存在当前浏览器本地。不同手机或浏览器之间不会自动同步本地新增数据。
